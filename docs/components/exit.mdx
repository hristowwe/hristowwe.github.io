---
id: exit
title: Командата exit
slug: /components/exit
---

import BrowserWindow from "@site/components/BrowserWindow";

```js
   if (command.equals("exit")) {
                break;
            }
```

### Как работи

Този код проверява дали командата е `exit`. Ако е така, изпълнението на цикъла се прекратява чрез оператора break, който излиза програмата.

<BrowserWindow url={`Пример за exit`}>
Enter command: exit

Press any key .......

</BrowserWindow>
